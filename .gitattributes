# Normalize line endings. Shell scripts MUST stay LF so they run in Linux
# containers; everything else is normalized to LF in the repo.
* text=auto eol=lf
*.sh text eol=lf
*.ps1 text eol=crlf
*.png binary
*.jpg binary
*.ico binary
